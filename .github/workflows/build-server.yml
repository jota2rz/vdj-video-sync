name: Build Server

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    name: Server – ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows x64
            os: windows-latest
            goos: windows
            goarch: amd64
            binary: vdj-video-sync-server.exe
          - name: macOS arm64
            os: ubuntu-latest
            goos: darwin
            goarch: arm64
            binary: vdj-video-sync-server
          - name: macOS amd64
            os: ubuntu-latest
            goos: darwin
            goarch: amd64
            binary: vdj-video-sync-server
          - name: Linux x64
            os: ubuntu-latest
            goos: linux
            goarch: amd64
            binary: vdj-video-sync-server

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: server/go.mod
          cache-dependency-path: server/go.sum

      - name: Install templ CLI
        run: go install github.com/a-h/templ/cmd/templ@latest

      - name: Install Tailwind CSS (Linux)
        if: runner.os != 'Windows'
        run: |
          curl -sLO "https://github.com/tailwindlabs/tailwindcss/releases/latest/download/tailwindcss-linux-x64"
          chmod +x tailwindcss-linux-x64
          sudo mv tailwindcss-linux-x64 /usr/local/bin/tailwindcss

      - name: Install Tailwind CSS (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $url = "https://github.com/tailwindlabs/tailwindcss/releases/latest/download/tailwindcss-windows-x64.exe"
          Invoke-WebRequest -Uri $url -OutFile "$env:USERPROFILE\go\bin\tailwindcss.exe"

      - name: Generate templ files
        working-directory: server
        run: templ generate

      - name: Build Tailwind CSS
        working-directory: server
        run: tailwindcss -i static/css/input.css -o static/css/output.css --minify

      # All targets use pure Go cross-compilation (no CGO).
      # macOS binaries are cross-compiled from Linux to avoid macOS runner
      # OOM issues with the large concentus/SILK package.
      # -p 1 limits parallel compilation to reduce peak memory usage.
      # The SILK codec package needs excessive RAM to compile, so we add
      # 8GB swap on Linux runners (7GB RAM) to prevent OOM kills (exit 143).
      # GOMEMLIMIT caps the Go compiler's heap, forcing aggressive GC.
      - name: Add swap space (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo swapoff /swapfile || true
          sudo rm -f /swapfile
          sudo fallocate -l 8G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          echo "Swap enabled:" && swapon --show
          free -h

      - name: Build
        working-directory: server
        shell: bash
        env:
          CGO_ENABLED: "0"
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          GOMEMLIMIT: 6GiB
        run: |
          echo "=== Memory before build ==="
          free -h
          go build -v -p 1 \
            -gcflags="github.com/lostromb/concentus/...=-N -l" \
            -o ${{ matrix.binary }} .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: server-${{ matrix.goos }}-${{ matrix.goarch }}
          path: server/${{ matrix.binary }}
          retention-days: 30
