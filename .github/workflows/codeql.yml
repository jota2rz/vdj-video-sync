name: CodeQL

on:
  push:
    branches: [main]
    paths-ignore:
      - "*.md"
      - "docs/**"
      - "previous-prototype/**"
  pull_request:
    branches: [main]
    paths-ignore:
      - "*.md"
      - "docs/**"
      - "previous-prototype/**"
  schedule:
    - cron: "0 6 * * 1" # Weekly on Monday at 06:00 UTC

permissions:
  security-events: write
  contents: read

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: go
            os: ubuntu-latest
            build-mode: manual
          - language: c-cpp
            os: windows-latest
            build-mode: manual
          - language: javascript-typescript
            os: ubuntu-latest
            build-mode: none
          - language: actions
            os: ubuntu-latest
            build-mode: none

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Go must be installed before CodeQL init so the tracer can wrap it
      - name: Set up Go
        if: matrix.language == 'go'
        uses: actions/setup-go@v5
        with:
          go-version-file: server/go.mod

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      # ── Go build (minimal — no templ/tailwind needed) ──
      - name: Build Go (CodeQL traced)
        if: matrix.language == 'go'
        working-directory: server
        run: go build -p 1 -gcflags="github.com/lostromb/concentus/...=-N -l" -o /dev/null ./...

      # ── C++ build (Windows — VDJ SDK requires Win32 types) ──
      - name: Download VirtualDJ SDK
        if: matrix.language == 'c-cpp'
        shell: pwsh
        run: |
          Invoke-WebRequest -Uri "https://www.virtualdj.com/developers/VirtualDJ8_SDK_20211003.zip" -OutFile sdk.zip
          Expand-Archive sdk.zip -DestinationPath VirtualDJ8_SDK_20211003 -Force

      - name: Download cpp-httplib
        if: matrix.language == 'c-cpp'
        shell: pwsh
        run: |
          Invoke-WebRequest -Uri "https://raw.githubusercontent.com/yhirose/cpp-httplib/v0.32.0/httplib.h" -OutFile plugin/vendor/httplib.h

      - name: Build C++ plugin (CodeQL traced)
        if: matrix.language == 'c-cpp'
        shell: pwsh
        run: |
          cd plugin
          cmake -B build -A x64
          cmake --build build --config Release

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"
